<?php 
    // Arithemetic operators
    // + - * / ** %

    // Increment/decrement operators
    // ++ --

    // Operator Precedence

    $x = 10;
    $y = 5;
    $result = null;

    // $result = $x + $y;
    // $result = $x - $y;
    // $result = $x * $y;
    // $result = $x / $y;
    // $result = $x ** $y;
    // $result = $x % $y;

    echo $result;


    $counter = 10;

    // $counter = $counter + 1;
    // $counter += 1;
    // $counter++;

    // $counter = $counter - 1;
    // $counter -= 1;
    // $counter--;


    echo $counter;

    $total = 1 + 2 - 3 * 4 / 5 ** 6;

    echo $total;

    

?>
