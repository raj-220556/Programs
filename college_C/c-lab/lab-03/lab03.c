#include <stdio.h>
int main(){
    // // 1...sum,product and mean of array
    // int i,j;
	// int n,mean;
	// int sum=0;
	// int product=1;
	// printf("Enter the range of the array : ");
	// scanf("%d",&n);
	// int a[n];
	// for(i=0;i<n;i++)
	// {
	// 	printf("Entered the value : ");
	// 	scanf("%d",&a[i]);
	// 	sum=sum+a[i];
	// 	product=product*a[i];
	// }
	// mean=sum/n;
	// printf("The sum of the all elements in the array is %d\n",sum);
	// printf("The product of the all elements in the array is %d",product);
	// printf("The mean value of the array is %d",mean);


    // // 2...2nd smallest and 2nd largest
    //  int n1;
    //  printf("Enter the no of arrays to be entered : ");
    //  scanf("%d",&n1);
    //  int e[n1],b1,s;
    //  int x,y;
    //  for(x=0;x<n1;x++)
    //  {
    //      printf("Enter the elements :");
    //      scanf("%d",&e[x]);
    //  }
    //  for(x=0;x<n1;x++)
    //  {
    //     for(y=0;y<n1-1;y++)
    //     {
    //         if(e[y]<e[y+1])
    //         {
    //         b1=e[y];
    //         e[y]=e[y+1];
    //         e[y+1]=b1;
    //         }
    //     }
    //  }
    //  for(x=0;x<n1;x++)
    //  {
    //  	 printf("The sorted elements are the %d\n",e[x]);
	//  }
	//  printf("The second highest in this array %d\n",e[1]);
    //  printf("The second smallest in this array %d\n",e[n1-2]);


    // // 3...addition and substraction of matrix
    // int u,v;
    //  int c[10][10];
    //  int d[10][10];
    //  int e[10][10];
    //  int m,n;
    //  int m1,n1;
    //  printf("Enter the no of rows of a matrix : ");
    //  scanf("%d",&m);
    //  printf("Enter the no of columns of a matrix : ");
    //  scanf("%d",&n);
    //  for(u=0;u<m;u++)
    //  {
    //  	for(v=0;v<n;v++)
    //  	{
    //  		printf("Enter the element into the matrix :");
    //  		scanf("%d",&c[u][v]);
	// 	 }
	//  }
	//  printf("Enter the no of rows of a matrix : ");
    //  scanf("%d",&m1);
    //  printf("Enter the no of columns of a matrix : ");
    //  scanf("%d",&n1);
    //  for(u=0;u<m1;u++)
    //  {
    //  	for(v=0;v<n1;v++)
    //  	{
    //  		printf("Enter the element into the matrix :");
    //  		scanf("%d",&d[u][v]);
	// 	 }
	//  }
	//  if(m==m1 && n==n1)
	//  {
	//  	for(u=0;u<m;u++)
	//  	{
	//  		for(v=0;v<n;v++)
	//  		{
	// 		 e[u][v]=c[u][v]+d[u][v];
	// 		 }
	// 	 }
	//  }
	//  for(u=0;u<m;u++)
	//  {
	//  	for(v=0;v<n;v++)
	//  	{
	//  		printf("The elements of additioned matrices are %d\n",e[u][v]);
	// 	 }
	//  }


    // // 4...multipilication of matrices
    // 	 int u,v,k;
    //  int c[10][10];
    //  int d[10][10];
    //  int e[10][10];
    //  int m,n;
    //  int m1,n1;
    //  printf("Enter the no of rows of a matrix : ");
    //  scanf("%d",&m);
    //  printf("Enter the no of columns of a matrix : ");
    //  scanf("%d",&n);
    //  for(u=0;u<m;u++)
    //  {
    //  	for(v=0;v<n;v++)
    //  	{
    //  		printf("Enter the element into the matrix :");
    //  		scanf("%d",&c[u][v]);
	// 	 }
	//  }
	//  printf("Enter the no of rows of a matrix : ");
    //  scanf("%d",&m1);
    //  printf("Enter the no of columns of a matrix : ");
    //  scanf("%d",&n1);
    //  for(u=0;u<m1;u++)
    //  {
    //  	for(v=0;v<n1;v++)
    //  	{
    //  		printf("Enter the element into the matrix :");
    //  		scanf("%d",&d[u][v]);
	// 	 }
	//  }
	//  if(m==n1)
	//  {
	//  	for(u=0;u<m;u++)
	//  	{
	//  		for(v=0;v<n1;v++)
	//  		{
	//  			for(k=0;k<n1;k++)
	//  			{
	//  				e[u][v]+=c[u][k]*d[k][v];
	// 			 }
	// 		 }
	// 	 }
	//  }
	//  for(u=0;u<m;u++)
	//  {
	//  	for(v=0;v<n1;v++)
	//  	{
	//  		printf("The elements of multiplication matrices are %d\n",e[u][v]);
	// 	 }
    //  }


    // // 5...transpose of matrix
    //  int rows, cols;
    // int i,j;
    // // Input the dimensions of the matrix
    // printf("Enter the number of rows and columns: ");
    // scanf("%d %d", &rows, &cols);
    //  int matrix[rows][cols], transposed[rows][cols];
    // // Input the matrix elements
    // printf("Enter the elements of the matrix:\n");
    // for (i = 0; i < rows; i++) {
    //     for (j = 0; j < cols; j++) {
    //     	printf("Enter the element A[%d][%d] :",i,j);
    //         scanf("%d", &matrix[i][j]);
    //     }
    // }

    // // Transpose the matrix
    // for (i = 0; i < rows; i++) {
    //     for (j = 0; j < cols; j++) {
    //         transposed[j][i] = matrix[i][j];
    //     }
    // }

    // // Print the transposed matrix
    // printf("Transposed Matrix:\n");
    // for (i = 0; i < cols; i++) {
    //     for (j = 0; j < rows; j++) {
    //         printf("%d ", transposed[i][j]);
    //     }
    //     printf("\n");
    // }	





    

}